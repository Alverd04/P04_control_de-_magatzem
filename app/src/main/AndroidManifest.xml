<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.almacen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Almacen">
        <activity android:name=".Views.AddStock"></activity>
        <activity android:name=".Views.RetrieveStock" />
        <activity android:name=".Views.HistorialMoviments" />
        <activity android:name=".Views.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.CreateArticle" />
        <activity android:name=".Components.ArticleLayout" />
        <activity android:name=".Views.MainActivity" />
    </application>

</manifest>